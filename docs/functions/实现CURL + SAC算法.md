现在正式开始CURL的复现：实现CURL + SAC算法（不考虑AC算法，因为它是在线策略算法）。

重点是实现CURL的表征模型即编码器，而由文献原文可知，编码器的架构与SAC等算法的视觉感知架构是相同的（即相同的卷积神经网络架构），故可以参考SAC算法的相关代码。

注意，CURL + SAC算法不是在已经实现的SAC算法上进行修改，而是单独作为一个算法，相关代码保存到`src/curl-sac/`下，不考虑代码共享，直接平行复制即可。

后续将CURL + SAC算法记为CURL算法，CURL算法的训练与AC、SAC的训练设计保持一致。

注意，CURL算法的具体架构、对比学习的具体设计、相关超参数以文献原文为主，特别是文献中4.7. CURL Contrastive Learning Pseudocode的伪代码。