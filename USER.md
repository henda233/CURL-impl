# USER

## 认知水平与知识掌握情况

- 用户是项目作者，熟悉强化学习领域的算法概念（AC、SAC、DQN、CURL、on-policy/off-policy、表征学习），对话中无需解释基础术语。
- 对项目代码库与 docs 目录内容有全局把握，理解算法实现的技术细节与验收口径。
- 熟悉 Windows 与 NVIDIA GPU 环境下的 CUDA、PyTorch 安装配置，能独立在 GPU 主机完成深度学习环境搭建。
- 对 uv 不熟悉，需要逐步讲解命令与清单文件原理。
- 熟悉 Linux 无头 GPU 服务器渲染后端问题域（GLFW/EGL/OSMesa 差异与取舍），能自行完成 EGL 实机验证；关注训练性能瓶颈，主张先静态分析、再分段计时量化的验证路径。

## 习惯与偏好

- 偏好适中颗粒度的文档：现状与历史各一段，每个任务记录 5 至 10 行，不追求详尽的长篇文档。
- 任务以算法/特性为边界划分，同一算法的完整过程记为一次历史任务。
- 记录采用"结论加链接"的方式：MEMO 只写简短结论并链接到 docs 下具体文档，不重复细节。
- 历史记录以 git 提交与 docs 文档为依据，不添加推测性内容。
- 文档链接使用仓库内相对路径。
- 已选择直接按 AGENTS.md 板块组织 MEMO.md 结构，不需要额外自定义字段。
- 当前任务、待办事项等信息由用户主动补充，不替用户推测下一步。
- 实施前习惯多轮讨论对齐（方案、范围、测量口径、结果的定位），拍板后一次到位；实现范围偏好 AC/SAC 等平行结构同步改动。
- 耗时的实机运行与完整剖析由用户自己承担（GPU 服务器与本机两端），agent 承担代码实现与本机快速验证；实机结论以用户回执为准。
- 文档更新节奏由用户控制：任务中途可要求暂缓更新 MEMO/USER，收尾阶段再统一更新。

## 规范、规则与约定

- 维护约定：每次任务结束后更新 MEMO.md；每轮提问后更新 USER.md。
- 记忆文件板块：项目现状、当前任务、历史任务介绍、笔记、其他重要信息。
- USER.md 只记录长期稳定的用户档案：认知水平、习惯偏好、规范约定、以往教训、决策风格；不得添加"本轮任务"之类的临时板块，任务决策、进展与主机事实按要求写入 MEMO.md。

## 以往教训

- 曾向 USER.md 添加"本轮任务相关约定"临时板块，违反 AGENTS.md 的板块规范，被用户要求改正。应向 USER 只记长期用户档案，任务性内容一律归 MEMO。

## 决策风格

- 决策快速明确，偏好提供选项后直接选择；涉及项目推进顺序与内容范围时由用户拍板。
- 默认接受推荐项中的保守、可逆选项（如自动检测而非硬编码、AC/SAC 范围同步覆盖），拒绝会破坏现有本机流程的方案。
- 偏好运行行为的显式可控：参数语义须由用户显式指定（如 `--gpu` 改为显式才启用 CUDA），不接受未约定即引入的隐式自动行为；对历史设计存疑时要求以提交与文档证据核实后再决策。
