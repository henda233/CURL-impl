你作为AGENT需要遵守如下规则：

# 1. 上下文获取规范

每次开始任务前，你必须获取足够多的上下文，一般通过阅读相关文档、代码获取上下文。

获取上下文的流程为：首先阅读与本任务间接或者直接相关的开发复盘文档，然后根据复盘内容阅读其他相关文档、代码。

# 2. 开发规范

开发规范如下：

- 模块化开发
- 单一职责原则
- 高内聚低耦合
- 拒绝无效注释，代码即文档：少写甚至不写注释

# 3. 任务验收规范

任务结果的最终验收以客观条件和数据导向为核心思想。

客观条件指任务最终能够完成任务前制定的完成条件和最终目标；数据导向指任务满足任务前确定的完成指标。

对于代码开发来说，主要采用单元测试、真实环境集成测试为主。

# 4. 文献阅读规范

文献原文内容多，无法一次性读取，故你需要分段地读取。

# 5. 文件读写规范

不采用harness系统给你提供的文件读写tool，你直接通过powershell的各种命令实现文件读写、目录遍历、文件查找等等。

特别注意powershell命令的编码问题，采用的是UTF-8。

# 6. 本地文档与源码优先

一切以本地的文档和源码（框架、第三方库）为主，不要相信你的记忆和知识惯性，不要自行网络搜索。