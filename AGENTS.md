你作为AGENT需要遵守如下规则：

# 0. 维护记忆

每次任务结束后，你需要在项目的根目录下创建或者更新`MEMO.md`记忆文件，该文件内容包括：项目现状（开发进度）、当前任务（即将或者正在进行的任务）、历史任务介绍（任务内容、完成流程、完成情况和总结）、笔记和其他重要信息。

# 1. 上下文获取规范

每次开始任务前，你必须获取足够多的上下文，一般通过阅读记忆文件，然后依次读取相关文档、源码。

获取上下文的流程为：首先立刻阅读记忆文件`MEMO.md`，然后阅读记忆文件里提及的、与当前任务相关的文档、源码。

# 2. 开发规范

开发规范如下：

- 模块化开发
- 单一职责原则
- 高内聚低耦合
- 不写任何注释，代码即文档

# 3. 任务验收规范

任务结果的最终验收以客观条件和数据导向为核心思想。

客观条件指任务最终能够完成任务前制定的完成条件和最终目标；数据导向指任务满足任务前确定的完成指标。

对于代码开发来说，主要采用单元测试、真实环境集成测试为主。

# 4. 文献阅读规范

文献原文内容多，无法一次性读取，故你需要分段地读取。

# 5. 本地文档与源码优先

一切以本地的文档和源码（框架、第三方库）为主，不要相信你的记忆和知识惯性，不要自行网络搜索。

# 6. 用语遣词造句规范

与用户交流、编写文章时必须遵守如下规范：

- 禁止使用关键词拼接（如 xxx + yyy + zzz）
- 禁止使用莫名其妙、毫无根据的自创词汇
- 禁止频繁使用口语、俗语，严格地使用语意明确、上下文明确清晰的书面语

# 7. 主动提问对齐颗粒度

在正式开始用户提交给你的任务前，主动向用户就当前任务进行多轮地、由浅入深地提问，并且问题要符合用户认识水平和能力，要提供完备的、明确的上下文（问题背景、问题本身）。

不要采用harness系统为你提供的提问相关功能、技能和工具，而是直接以聊天对话的方式向用户提问，并且你可以一轮对话中提出多个问题。你需要为每个问题提供三个选项以便用户方便的选择，当然你也要接受用户的自定义回答。

向用户提问的根本目的是降低你与用户之间的信息差，对齐颗粒度。你必须假设你之前学习到的一切知识、事物是过时的，甚至是与当前任务相互违背的，所以你必须向用户提问。

# 8. 认识用户

每当你向用户提问后，你需要在当前项目根目录下创建或者更新`USER.md`，该文件内容包括：用户的认知水平和相关知识掌握情况、用户的习惯和偏好、用户需要你记住的规范规则与约定、用户之前犯下的错误和得到的教训、用户的决策风格。

`USER.md`将帮助你更好地了解和辅助用户，降低你与用户之间的信息差，降低沟通成本。